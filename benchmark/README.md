# LLM Inference Benchmark

Инструменты для сравнения производительности vLLM и llama.cpp.

## 📊 Метрики

### TTFT (Time to First Token)
Время от отправки промпта до получения первого токена.
- **Измеряется**: в миллисекундах (ms)
- **Важность**: Критично для интерактивных приложений (чатботы, coding assistants)
- **Факторы влияния**: длина промпта, размер batch, prefill optimization

### TPS (Tokens Per Second)
Скорость генерации выходных токенов.
- **Измеряется**: tokens/second
- **Важность**: Основная метрика пропускной способности
- **Факторы влияния**: размер модели, квантизация, GPU/CPU balance

### ITL (Inter-Token Latency)
Средняя задержка между последовательными токенами.
- **Измеряется**: в миллисекундах (ms)
- **Формула**: (E2E Time - TTFT) / (Output Tokens - 1)
- **Важность**: Показывает консистентность генерации

### E2E Latency (End-to-End)
Полное время от запроса до завершения генерации.
- **Измеряется**: в миллисекундах (ms)
- **Формула**: TTFT + (Output Tokens × ITL)
- **Важность**: Воспринимаемая пользователем latency

## 🚀 Использование

### 1. Раздельное тестирование (рекомендуется)

Из-за ограничений GPU памяти, рекомендуется тестировать бэкенды по отдельности:

#### Шаг 1: Тест vLLM

```bash
# Терминал 1: Запуск vLLM сервера
source venv-vllm/bin/activate
vllm serve models/hf/Qwen3-8B-AWQ --port 8000

# Терминал 2: Запуск бенчмарка
source venv-vllm/bin/activate
# (опционально) cd в корень проекта

python benchmark/benchmark.py \
    --backend vllm \
    --scenario "qwen3-8b-gpu" \
    --prompt all \
    --warmup 5 \
    --runs 20
```

#### Шаг 2: Тест llama.cpp

```bash
# Остановить vLLM (Ctrl+C в терминале 1)

# Терминал 1: Запуск llama.cpp сервера
./llama.cpp/build/bin/llama-server \
    --model models/gguf/Qwen3-8B-Q4_K_M.gguf \
    --n-gpu-layers -1 \
    --port 8001

# Терминал 2: Запуск бенчмарка
source venv-vllm/bin/activate
# (опционально) cd в корень проекта

python benchmark/benchmark.py \
    --backend llamacpp \
    --scenario "qwen3-8b-gpu" \
    --prompt all \
    --warmup 5 \
    --runs 20
```

#### Шаг 3: Сравнение результатов

```bash
python benchmark/compare_results.py \
    benchmark/results/benchmark_qwen3-8b-gpu_*.json \
    benchmark/results/benchmark_qwen3-8b-gpu_*.json \
    --backend1 vllm \
    --backend2 llamacpp
```

### 2. Одновременное тестирование (если достаточно памяти)

Если у вас >24GB VRAM или используете маленькие модели:

```bash
# Терминал 1: vLLM на порту 8000
vllm serve models/hf/Qwen3-8B-AWQ --port 8000 --gpu-memory-utilization 0.4

# Терминал 2: llama.cpp на порту 8001
./llama.cpp/build/bin/llama-server --model models/gguf/Qwen3-8B-Q4_K_M.gguf --n-gpu-layers -1 --port 8001

# Терминал 3: Бенчмарк обоих
python benchmark/benchmark.py \
    --backend both \
    --scenario "qwen3-8b-comparison" \
    --prompt all
```

## 📝 Параметры benchmark.py

```bash
python benchmark/benchmark.py [OPTIONS]

Options:
  --backend <vllm|llamacpp|both>  Какой бэкенд тестировать (обязательно)
  --vllm-url <url>                vLLM server URL (default: http://localhost:8000)
  --llamacpp-url <url>            llama.cpp server URL (default: http://localhost:8001)
  --prompt <short|medium|long|all> Тип промпта (default: all)
  --warmup <n>                    Количество прогревочных запросов (default: 5)
  --runs <n>                      Количество измерительных запросов (default: 20)
  --output <path>                 Путь для сохранения JSON результатов
  --scenario <name>               Имя сценария для результатов (default: custom)
```

## 📋 Типы промптов

### Short
- **Промпт**: ~10 токенов
- **Генерация**: 100 токенов
- **Описание**: "Explain quantum computing in simple terms."
- **Цель**: Минимальный overhead, быстрая генерация

### Medium
- **Промпт**: ~100 токенов
- **Генерация**: 300 токенов
- **Описание**: Анализ текста о развитии AI
- **Цель**: Реалистичный use case для чат-ботов

### Long
- **Промпт**: ~200 токенов
- **Генерация**: 500 токенов
- **Описание**: Детальное техническое задание на архитектуру e-commerce
- **Цель**: Стресс-тест с длинным контекстом

## 📂 Результаты

Результаты сохраняются в `results/benchmark_<scenario>_<timestamp>.json`:

```json
{
  "timestamp": "2026-01-09T03:00:00",
  "scenario": "qwen3-8b-gpu",
  "config": {
    "warmup_runs": 5,
    "measurement_runs": 20
  },
  "results": {
    "vllm": {
      "model": "Qwen/Qwen3-8B-AWQ",
      "prompts": {
        "short": {
          "ttft_ms": {"mean": 45.2, "std": 5.1, "p50": 44.0, "p95": 55.0},
          "tps": {"mean": 85.3, "std": 3.2, "p50": 85.0, "p95": 90.0},
          "e2e_latency_ms": {"mean": 1250.5, "std": 50.0},
          "successful_runs": 20,
          "total_runs": 20
        }
      }
    }
  }
}
```

## 🔍 Интерпретация результатов

### TTFT (Time to First Token)

**Хороший результат**: < 100ms
- vLLM обычно: 30-80ms
- llama.cpp обычно: 50-150ms

**Что влияет**:
- Длина промпта (линейная зависимость)
- Prefill optimization (vLLM использует Flash Attention)
- Batch size (больше = медленнее TTFT)

### TPS (Tokens Per Second)

**Хороший результат**:
- Small models (7-8B): 50-150 tokens/sec
- Large models (32B+): 10-40 tokens/sec

**Что влияет**:
- Квантизация (Q4 быстрее Q8, но хуже качество)
- GPU offloading (больше слоёв в GPU = быстрее)
- Memory bandwidth (основной bottleneck)

### Сравнение бэкендов

**vLLM обычно быстрее когда**:
- Batch inference (несколько запросов параллельно)
- Длинные последовательности
- Модель полностью в GPU

**llama.cpp обычно быстрее когда**:
- Single request latency критична
- CPU/GPU гибридный режим
- Агрессивная квантизация (Q4, Q2)

## 📊 Пример результатов

### Qwen3-8B (полностью в GPU)

| Метрика | vLLM (AWQ 4-bit) | llama.cpp (Q4_K_M) | Winner |
|---------|------------------|---------------------|---------|
| TTFT (short) | 42ms | 75ms | vLLM |
| TPS (short) | 95 tok/s | 88 tok/s | vLLM |
| E2E (short) | 1100ms | 1200ms | vLLM |
| TTFT (long) | 180ms | 320ms | vLLM |
| TPS (long) | 82 tok/s | 85 tok/s | llama.cpp |

### Qwen3-32B (CPU offload)

| Метрика | vLLM (FP8) | llama.cpp (Q8_0) | Winner |
|---------|------------|-------------------|---------|
| TTFT | 250ms | 180ms | llama.cpp |
| TPS | 18 tok/s | 25 tok/s | llama.cpp |
| E2E | 28s | 20s | llama.cpp |

**Вывод**: llama.cpp эффективнее при CPU offloading благодаря послойному распределению.

## 🛠️ Troubleshooting

### Все запросы failed

**Проблема**: `Error: All runs failed`

**Причины**:
1. Сервер не запущен
2. Неправильный порт
3. Сервер упал при загрузке

**Решение**:
```bash
# Проверить что сервер работает
curl http://localhost:8000/v1/models  # vLLM
curl http://localhost:8001/v1/models  # llama.cpp

# Проверить логи сервера на ошибки
```

### Высокая дисперсия результатов

**Проблема**: Большой `std` (standard deviation)

**Причины**:
1. Фоновые процессы на GPU
2. Thermal throttling
3. Недостаточно warmup runs

**Решение**:
```bash
# Увеличить warmup
python benchmark/benchmark.py --warmup 10 --runs 30

# Закрыть другие GPU процессы
nvidia-smi  # Проверить что ещё использует GPU

# Проверить температуру
nvidia-smi -l 1  # Мониторинг в реальном времени
```

### OOM во время бенчмарка

**Проблема**: Out of memory error

**Решение**:
```bash
# vLLM: уменьшить gpu-memory-utilization
vllm serve model --gpu-memory-utilization 0.7

# llama.cpp: уменьшить количество GPU слоёв
./llama.cpp/build/bin/llama-server --model model.gguf --n-gpu-layers 20

# Или тестировать по отдельности
```

## 📚 Дополнительные материалы

### Статьи о бенчмарках

- [NVIDIA LLM Benchmarking Fundamentals](https://developer.nvidia.com/blog/llm-benchmarking-fundamental-concepts/)
- [Anyscale Benchmarking Metrics](https://docs.anyscale.com/llm/serving/benchmarking/metrics)
- [vLLM Performance Blog](https://blog.vllm.ai/2023/06/20/vllm.html)

### Инструменты

- [NVIDIA GenAI-Perf](https://github.com/NVIDIA/GenAI-Perf) - Профессиональный инструмент бенчмарка
- [llm-perf-leaderboard](https://huggingface.co/spaces/optimum/llm-perf-leaderboard) - Публичный leaderboard

### Оптимизация

- [vLLM Performance Tuning](https://docs.vllm.ai/en/stable/configuration/optimization/)
- [llama.cpp Performance Guide](https://github.com/ggml-org/llama.cpp/discussions/2094)

---

**Метрики собираются**: Streaming API (более точное измерение TTFT)
**Статистика**: mean, std, p50, p95, p99
**Формат результатов**: JSON для дальнейшего анализа
